groups:
  - name: monitoring-dashboard_alerts
    rules:
      - alert: monitoring-dashboard_high_error_rate
        expr: rate(http_requests_total{status=~"5.."}[5m]) > 0.05
        for: 5m
        labels:
          severity: critical
          service: monitoring-dashboard
        annotations:
          summary: "High error rate in monitoring-dashboard"
          description: "Error rate is above 5% for the last 5 minutes"
      
      - alert: monitoring-dashboard_high_latency
        expr: histogram_quantile(0.95, rate(http_request_duration_seconds_bucket[5m])) > 1
        for: 5m
        labels:
          severity: warning
          service: monitoring-dashboard
        annotations:
          summary: "High latency in monitoring-dashboard"
          description: "95th percentile latency is above 1 second for the last 5 minutes"
      
      - alert: monitoring-dashboard_low_availability
        expr: up{job="monitoring-dashboard"} == 0
        for: 2m
        labels:
          severity: critical
          service: monitoring-dashboard
        annotations:
          summary: "monitoring-dashboard is down"
          description: "Service has been down for more than 2 minutes"
      
      - alert: monitoring-dashboard_high_cpu_usage
        expr: rate(process_cpu_seconds_total[5m]) > 0.8
        for: 5m
        labels:
          severity: warning
          service: monitoring-dashboard
        annotations:
          summary: "High CPU usage in monitoring-dashboard"
          description: "CPU usage is above 80% for the last 5 minutes"
      
      - alert: monitoring-dashboard_high_memory_usage
        expr: process_resident_memory_bytes / (1024 * 1024) > 512
        for: 5m
        labels:
          severity: warning
          service: monitoring-dashboard
        annotations:
          summary: "High memory usage in monitoring-dashboard"
          description: "Memory usage is above 512MB for the last 5 minutes"
      
      - alert: monitoring-dashboard_low_disk_space
        expr: (node_filesystem_avail_bytes{mountpoint="/"} / node_filesystem_size_bytes{mountpoint="/"}) < 0.2
        for: 5m
        labels:
          severity: warning
          service: monitoring-dashboard
        annotations:
          summary: "Low disk space for monitoring-dashboard"
          description: "Disk space is below 20% for the last 5 minutes"
